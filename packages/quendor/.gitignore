node_modules
dist
*.log
.DS_Store

# Acceptance goldens for copyrighted games (Infocom) are generated and diffed
# locally, never committed — a transcript is wall-to-wall game text. The solution
# files (command lists) in tests/ac/ are kept; the goldens are not.
tests/ac/*.golden.txt
